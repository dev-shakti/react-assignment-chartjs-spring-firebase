import { Box, Typography } from "@mui/material";

export const DashboardPage = () => {

  return (
    <Box>
      <Typography variant="h4">Welcome to the Dashboard</Typography>
      <Typography variant="body1">Hello, John</Typography>
    </Box>
  );
};